spring.application.name=backend

# Active environment: dev (default), or prod (override with SPRING_PROFILES_ACTIVE)
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

# JWT configuration
jwt.secret=${JWT_SECRET:qWjWYCSdW4h/NOfPgeEePtwmYqWr8lg+Vg7sSrKjLFP/X54o1JJX2dO5YGxM/zBM}
# Token validity in milliseconds (24 hours)
jwt.expiration=86400000

spring.jackson.serialization.fail-on-empty-beans=false

app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000}
